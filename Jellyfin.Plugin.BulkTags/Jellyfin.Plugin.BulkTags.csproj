<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Jellyfin 12 runs on .NET 10; the server loads this assembly in-process. -->
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Jellyfin.Plugin.BulkTags</RootNamespace>
    <!-- 2.x is the Jellyfin 12 line; 1.x remains the Jellyfin 10.11 build. The
         repository manifest offers both, and since targetAbi is only a minimum
         server version, a Jellyfin 12 server considers both entries compatible
         and picks the highest version - so this must stay above the 1.x line. -->
    <AssemblyVersion>2.0.0.0</AssemblyVersion>
    <FileVersion>2.0.0.0</FileVersion>
    <InformationalVersion>2.0.0</InformationalVersion>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <!-- ExcludeAssets="runtime" keeps the Jellyfin assemblies out of the plugin
         output. The server already has them loaded, and shipping private copies
         alongside the plugin breaks loading. -->
    <PackageReference Include="Jellyfin.Common" Version="12.0.0" ExcludeAssets="runtime" />
    <PackageReference Include="Jellyfin.Controller" Version="12.0.0" ExcludeAssets="runtime" />
    <PackageReference Include="Jellyfin.Model" Version="12.0.0" ExcludeAssets="runtime" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="Configuration\configPage.html" />
    <EmbeddedResource Include="Configuration\configPage.html" />
    <None Remove="Configuration\bulkTags.html" />
    <EmbeddedResource Include="Configuration\bulkTags.html" />
    <None Remove="Configuration\configuration.html" />
    <None Remove="manifest.json" />
    <None Include="manifest.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
